---
id: T0-CARD-TAKEOVER-2
title: The card takeover command with its guarantees stated to the limits of a store without compare-and-set, the three hardenings R3 decision 2 named, and the missing compare-and-set declared a non-goal (replacement of T0-CARD-TAKEOVER after its two R3 decisions)
status: todo
branch: T0-CARD-TAKEOVER-2
worktree: C:\wt\T0-CARD-TAKEOVER-2
allow_paths:
  - src/loop/card-runner.ts
  - src/cli/main.ts
  - tests/scenarios/two-windows.test.ts
  - tests/scenarios/t0-flow.test.ts
  - docs/OPERATIONS.md
  - docs/ARCHITECTURE.md
  - docs/REQUIREMENTS-TRACEABILITY.md
  - README.md
  - CHANGELOG.md
  - specs/tasks/T0-CARD-TAKEOVER.md
  - specs/tasks/T0-CARD-TAKEOVER-2.md
dod_command: npm run typecheck && node --test tests/scenarios/two-windows.test.ts tests/scenarios/t0-flow.test.ts tests/infra/lease.test.ts tests/surface/prose.test.ts tests/surface/mirror.test.ts
dod_exit: 0
requirements:
  - R1. WHEN the card lease of another session has expired and, at the reconciliation's own read, no operation of the card is unresolved in any goal, `aidlc card takeover <card>` shall take the lease at the next generation and shall record that generation on the card run.
  - R2. WHEN the lease record is missing or released, WHEN the acting session holds the lease at the generation the run carries, WHEN the lease of another session is live at the reconciliation's read, or WHEN an operation of the card is unresolved at that read, the takeover shall refuse and shall write nothing.
  - R3. After a takeover, a write of the previous owner fenced at the previous generation shall be refused, and `next` shall read the stored run, never a caller's snapshot.
  - R4. After a takeover, the card run shall carry the state selected from the stored run read once the lease is held, with an ownership stop cleared and any other stop kept as the owner's own `card next` keeps it.
  - R5. WHEN the acting session holds the card lease at a generation the run does not carry, the takeover shall complete the run update without taking the lease again, after reading the lease once more right before it journals and saves.
  - R6. The takeover shall journal a handoff intent before the lease write and shall resolve that intent and the acquisition record by card resource and lease generation across every goal's journal.
  - R7. WHEN an operation of the card is found unresolved right after the lease write, the takeover shall leave the run unchanged until that operation is reconciled.
  - R8. `next` shall evaluate its plan-checkpoint guard on the stored run.
  - R9. The changed documents shall state the takeover's guarantees to the limits of a store without compare-and-set, naming each remaining window and its recovery, and shall promise nothing beyond them.
acceptance:
  - 1. `CardRunner.takeover(goal, card, run)` takes the card lease through `LeaseStore.takeover` once the lease of another session has expired and, at the reconciliation's own read, no operation of the card is unresolved in any goal (`ops.list({ cardId })`, statuses intended, issued, running and UNKNOWN): the generation advances by one, the owner is the acting session, the operation is `card:<id>`, the run is saved with `ownerGeneration` equal to the new generation, and the journal carries the handoff intent (`NOTE`, `kind: card-takeover-intent`, the previous owner and both generations) sequenced before `LEASE_ACQUIRED` with `takeover: true`, `previousOwner` and `previousGeneration`; the previous owner's `fence` at the old generation throws `FencedError`; its `next` with the run it kept from before the takeover (generation 0, stale merge and closure evidence, no stop) writes nothing of that back, because `next` reads the stored run and never a caller's snapshot, so a blocking stop persisted there (risk in the scenario) stands over the stale dispatch and over the new owner's renewal, and once it is lifted the stale dispatch records only its ownership stop on the stored run, which the new owner's following `next` revalidates and clears (two-windows.test.ts "prepared"). [R1] [R3] [R6] [dod arm 1]
  - 2. Each refusal throws, names its cause and writes nothing (the run record, the lease record and the journal length are equal before and after): no lease record; a released lease; a lease this session holds at the generation the run carries; a live lease of another session (the store's message: expiry alone does not prove the owner stopped); an unresolved operation of the card in this goal or in another goal that lists it, named by id. A release, a takeover or an operation that lands between the command's first read and the reconciliation's own read is seen there, because the record the store hands to the reconciliation and the ledger are validated at that read: the released record refuses, the operation refuses by id, and a record already this session's is not advanced again. An operation admitted after the reconciliation's read and before the lease write cannot be refused before the write; it is found right after it: the lease stays taken, the run stays as it was (generation and stop unchanged, no acquisition journaled), the command names the id, and once the operation is reconciled the command run again completes the takeover (two-windows.test.ts "prepared", "refusals", "interleaved"). [R2] [R7] [dod arm 1]
  - 3. After the takeover the run carries the state that `card next` selects from the stored run, through the evidence gathering `next` and `takeover` share, on the run read again once the lease is held (a stop another process persisted before that read stays, at the new generation: STOP/risk in the scenario): a run PREPARE completed resumes at its state with the ownership stop cleared by the same renewal that revalidates an owner's expired lease (journal `LEASE_RENEWED` with `revalidated`), and the new owner's `next` continues it; a run interrupted between the lease claim and the PREPARE save (no `ownerGeneration`, no worktree) is owned at the new generation, selected as PREPARE, and the new owner's `next` returns the `prepare` directive with the lease renewed at that generation, not re-acquired; what lands after that read is the store's window named in acceptance 8 (two-windows.test.ts "prepared", "unprepared", "interleaved"). [R4] [dod arm 1]
  - 4. `aidlc card takeover <card> [--goal <id>]` runs the takeover on the stored run and prints `completed`, the new lease generation, the previous owner (on a completion, as recovered from the handoff intent, else absent) and the run state; a card without a run record exits non-zero and no record is created; a refusal exits non-zero with the cause on stderr and leaves both records unchanged; a run past its deadline is taken and prints the STOP/time `card next` would record; an interrupted takeover (the lease this session's, the run at the old generation) is completed through the command with `completed: true`, the generation unchanged and the previous owner from the intent, and a completion of a lease taken outside the command prints no previous owner (two-windows.test.ts "command"). [R1] [R2] [R5] [dod arm 1]
  - 5. A goal that the goal-level `next` stopped on the card's ownership stop is resumed after the card takeover: the dispatch then reports the taken-over card as running (a `wait` on `<card>:BUILD` for a run with a worktree; `run-card` for a run at PREPARE) and the new owner's `card next` continues it; no redispatch beyond that is promised (two-windows.test.ts "goal stopped"), and `docs/OPERATIONS.md` (Sessions; the STOP reasons row `ownership`), `README.md` (Multi-session), `docs/ARCHITECTURE.md` (state layer, coordination), `docs/REQUIREMENTS-TRACEABILITY.md` (MS2, Q23) and the CHANGELOG entry describe the takeover, its preconditions, its refusals, the completion, the resume and the dispatch after it (the sentences quoted in the ship evidence; prose.test.ts green). [R9] [dod arm 1]
  - 6. A lease this session already holds at a generation the run does not carry (a takeover or a claim whose run update did not land, including a second window of the same session that took the lease between the command's first read and the lease write) is completed without another advance: the lease is read once more right before the journal and the save, and a record that is released, another session's or at another generation by then refuses without a write; otherwise the run is saved at the lease generation and its state selected, the result carries `completed: true`, the previous owner is the one the handoff intent names when the lease was taken through the command (a lease taken outside it has none), and both the intent and the acquisition are resolved by card resource and lease generation across every goal's journal, so a takeover through one goal completed through another goal that lists the card names the previous owner and journals no second acquisition; a claim whose run update did not land is completed the same way at the claim's generation with no previous owner and starts at PREPARE; a completion refuses while an operation of the card is unresolved; a run already at the lease generation refuses, and `card next` renews it (two-windows.test.ts "interleaved", "completion", "across goals"). [R5] [R6] [dod arm 1]
  - 7. `next` reads the stored run before its plan-checkpoint guard: for a T2 goal in CARDS without the checkpoint, a caller snapshot that carries a stop the stored run does not no longer skips the guard, and `next` returns the `wait` on `goal:CARDS:plan-checkpoint` without dispatching (two-windows.test.ts "checkpoint"). [R8] [dod arm 1]
  - 8. The documents of acceptance 5 and this card name the store's limits and promise nothing beyond them: (a) the lease write, where a renewal by the old owner that lands between `LeaseStore.takeover`'s read and its write is overwritten (the primitive's window, shared with `aidlc goal takeover`; the old owner's next write is fenced); (b) the run write, where a writer that passed its fence before the lease write and saves after it (`recordAttempt`, the ship, a review decision, a stale dispatch) writes the run at the old generation, and the takeover's own save can lose a stop persisted after its read; the recovery is `aidlc card takeover` run again, which completes the run at the lease generation (the own-lease completion of acceptance 6), and a stop it lost is re-recorded by its author; (c) the journal, where two completions of the same session may both journal one generation's acquisition (the audit keys on generation); (d) the ledger, where an operation admitted after the takeover's last read is left to `next`'s own assessment (issued, running and UNKNOWN in the run's goal, as always), an intended one reconciled with `aidlc ops reconcile`. No document claims serialisation, a critical section, an atomic journal or a fence beyond the ones the writers already apply, and the non-goals list them (the sweep grep and the sentences quoted in the ship evidence). [R9] [dod arm 1]
budget: 760
tdd: true
sweep: "grep -rn 'card takeover' src tests docs README.md CHANGELOG.md specs: card-runner.ts (the two PREPARE ownership stops naming the card and goal ids; CardRunner.takeover), main.ts (the command), two-windows.test.ts (the takeover tests), docs/OPERATIONS.md:97 and :224, README.md:66 and :69, docs/ARCHITECTURE.md:12 and :14, CHANGELOG.md (the entry), specs/tasks/T0-CARD-TAKEOVER.md (superseded), specs/tasks/T0-SESSION-IDENTITY-2.md and -3.md (history; unchanged); grep -rn '\.takeover(' src: lease.ts (the primitive), main.ts (goal takeover), card-runner.ts (the CLOSE claim's own takeover, unchanged; CardRunner.takeover); grep -rn 'ops.unresolved\|ops.list' src/loop/card-runner.ts: assess (this goal, as next always did), close (this goal, unchanged), takeover (the card in every goal, twice: at the reconciliation and after the lease write); grep -rn 'compare-and-set\|critical section\|serialis' docs README.md CHANGELOG.md specs/tasks/T0-CARD-TAKEOVER-2.md: the limits stated, never a promise"
non_goals: [a compare-and-set or critical section in the state store for lease or run or journal writes, serialising the takeover with claim and renewal and release, fencing recordAttempt and the other run writers beyond the fence they already apply, changing what next assesses as an unresolved operation (statuses or goals), an automatic redispatch of a taken-over card (the dispatch reports it running and card next continues it), changing LeaseStore.takeover or the lease TTL, rewriting the RED history of T0-CARD-TAKEOVER, taking over the goal lease (aidlc goal takeover does that)]
forbid: [editing .aidlc state by hand, changing LeaseStore fencing or takeover semantics, taking over without reconciliation, copying any part of a lease file into hook output, a file lock around the store (docs/LESSONS.md 2026-09-14 T1-LOOP-LESSONS)]
hygiene: "The state store writes atomically but has no compare-and-set (`store.ts`: temp file and rename, exclusive create for claims); every takeover guarantee is stated to that limit, with the window and the recovery named, instead of narrowed once more. The three hardenings are the ones a review can pin: the stored run under the plan-checkpoint guard, the lease read once more before a completion journals and saves, and intent and acquisition resolved across goals. RED receipts: T0-CARD-TAKEOVER's first RED (a79f5f1) failed on the absent seam, every later slice on assertions against the callable takeover (154b7e3; .review/T0-CARD-TAKEOVER.red2.log, red3.log, red4.log); this card's slices are assertion-level REDs on that seam too. Fixture leases in the CLI-level test expire in 2020 and live until 2126 because the CLI runs on the wall clock. Recovery text is derived from the runner's conditions, per the T0-SESSION-IDENTITY-3 lesson."
doc_sync: docs/OPERATIONS.md (Sessions; STOP reasons row ownership), docs/ARCHITECTURE.md (state layer; coordination), docs/REQUIREMENTS-TRACEABILITY.md (MS2, Q23), README.md (Multi-session), CHANGELOG.md
---

# T0-CARD-TAKEOVER-2

## Deliverable
Replacement of T0-CARD-TAKEOVER (branch T0-CARD-TAKEOVER, 59eafe9: R2 cycle 1 passed; R3 decision 1 blocked on seven findings, repaired; R2 cycle 2 blocked three times and was exhausted; R3 decision 2 blocked on nine findings, most of them the same shape: a read-then-write window in a store without compare-and-set, probed one field at a time). This card carries the same command and closes the series by stating the guarantees to the store's limits instead of narrowing another window, plus the three hardenings the second decision named that a test can pin.

`CardRunner.takeover(goal, card, run)` and `aidlc card takeover <card> [--goal <id>]`: once the lease of another session has expired and no operation of the card is unresolved in any goal, the lease is taken at the next generation (the old owner fenced), the run records that generation, and the card state is selected again from the stored run; the ownership stop is cleared by the renewal that already revalidates an owner's expired lease, and any other stop stays. A handoff intent precedes the lease write, one acquisition per generation follows it, and both are resolved by card resource and generation across every goal's journal. An own lease at a generation the run does not carry is an interrupted takeover or claim, completed after the lease is read once more. `next` reads the stored run, before its plan-checkpoint guard too, so no caller's snapshot writes anything back.

## Limits (a store without compare-and-set)
1. The lease write: a renewal by the old owner that lands between `LeaseStore.takeover`'s read and its write is overwritten; the old owner's next write is fenced. The primitive's window, shared with `aidlc goal takeover`.
2. The run write: a writer that passed its fence before the lease write and saves after it (`recordAttempt`, the ship, a review decision, a stale dispatch) writes the run at the old generation, and the takeover's own save can lose a stop persisted after its read. Recovery: `aidlc card takeover` run again completes the run at the lease generation; a lost stop is re-recorded by its author.
3. The journal: two completions of the same session may both journal one generation's acquisition. The audit keys on generation.
4. The ledger: an operation admitted after the takeover's last read is left to `next`'s own assessment (issued, running and UNKNOWN in the run's goal, as always); an intended one is reconciled with `aidlc ops reconcile`.

## Operator procedure (derived from the runner's conditions)
1. `aidlc card status <card> --goal <id>` prints the lease (owner session and host, generation, expiry) and the run's `ownerGeneration`.
2. While the lease is live, the only continuation is the owner identity: `AIDLC_SESSION=<lease.owner.session>` on the same host, when `ownerGeneration` equals `lease.generation`.
3. Once the lease has expired and no operation of the card is unresolved in any goal: `aidlc card takeover <card> --goal <id>`. A refusal names its cause and writes nothing; a late operation leaves the lease taken and the run untouched, and the command run again completes once it is reconciled.
4. If the goal-level `aidlc next` stopped the goal on the card's ownership stop meanwhile: `aidlc goal resume <id> --reason "<why>"`; the dispatch then reports the card running.
5. `aidlc card next <card>`: a run PREPARE completed resumes at its state; a run without a worktree starts at PREPARE.

## Acceptance (DoD = command + exit code + assertion; paired with the closed `acceptance:` list)
```powershell
npm run typecheck && node --test tests/scenarios/two-windows.test.ts tests/scenarios/t0-flow.test.ts tests/infra/lease.test.ts tests/surface/prose.test.ts tests/surface/mirror.test.ts
```
- Expected exit code: 0
- Assertion: the prepared, unprepared, refusals, command, goal-stopped, interleaved, completion, across-goals and checkpoint takeover tests pass; the existing two-window, pre-review gate, heartbeat, lease, prose and mirror tests stay green.
