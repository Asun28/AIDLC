---
id: T0-SHIP-BASE-SYNC-2
title: The GitHub ship path syncs the candidate with its base before any remote effect, so a conflict returns the card to BUILD with the merge left in the worktree instead of a PR GitHub never runs CI on (replacement of T0-SHIP-BASE-SYNC after its two R3 decisions)
status: todo
branch: T0-SHIP-BASE-SYNC-2
worktree: C:\wt\T0-SHIP-BASE-SYNC-2
allow_paths:
  - src/delivery/github-ship.ts
  - src/delivery/ship.ts
  - tests/infra/github-ship.test.ts
  - tests/infra/ship.test.ts
  - README.md
  - docs/ARCHITECTURE.md
  - docs/OPERATIONS.md
  - CHANGELOG.md
  - specs/tasks/T0-SHIP-BASE-SYNC.md
  - specs/tasks/T0-SHIP-BASE-SYNC-2.md
dod_command: npm run typecheck && node --test tests/infra/github-ship.test.ts tests/infra/ship.test.ts tests/scenarios/t0-flow.test.ts
dod_exit: 0
requirements:
  - R1. WHEN the GitHub ship path has a candidate-bound verdict for the committed head, it shall fetch the base and test the merge of the base into the head before any push, PR or local merge.
  - R2. WHEN the test merge reports a conflict, the ship path shall start the same merge in the card worktree (never committing it), print git's own conflict diagnostic on its output and fail the ship as merge-failed, so the card runner returns the card to BUILD naming merge-conflicts.
  - R3. WHEN the test merge is clean, the ship path shall continue with the head unchanged, so the reviewed candidate is the commit that is pushed, gated and merged.
  - R4. WHEN the base cannot be fetched or resolved, or the merge test fails for a reason other than a conflict, the ship path shall fail as merge-failed without a conflict diagnostic, which the runner stops with reason tool and the resume command.
  - R5. WHEN the merge started on a conflict does not leave a merge in progress, the ship path shall publish none of its lines as a diagnostic and fail as in R4.
  - R6. WHEN the main checkout does not have the local base checked out, the local ship shall fail before the sync and before any merge.
  - R7. WHEN a merged PR already exists for the branch, the remote ship shall return merged before the sync, without starting a merge.
acceptance:
  - 1. `GitHubShipPath.ship` runs the base sync after the verdict check and before any remote effect, in both modes. `<base>` is `req.base` with one `origin/` prefix stripped, normalised exactly once and used for the PR target, the fetch refspec and the tested ref alike (a branch literally named `origin/main` is one branch throughout; tested in both modes). Remote mode: `git fetch --quiet --no-tags origin +refs/heads/<base>:refs/remotes/origin/<base>` in the worktree, then `refs/remotes/origin/<base>` resolved as a commit (`git rev-parse --verify --quiet <ref>^{commit}`); local mode: no fetch, `refs/heads/<base>` (the branch the local merge targets). Then `git merge-tree --write-tree HEAD <ref>` in the worktree, exit 0 meaning clean, exit 1 a conflict, anything else a failure. github-ship.test.ts asserts the fetch, the resolve and the merge-tree calls in that order, all before `git push`, and that local mode issues no fetch. [R1] [dod arm 1]
  - 2. On a conflict the path runs `git merge --no-ff --no-commit <ref>` in the worktree with `LC_ALL=C` in the child environment (so the diagnostic is the English line the runner matches), appends every stdout and stderr line of that merge to the ship output (`Auto-merging ...`, `CONFLICT (content): Merge conflict in <path>`, `Automatic merge failed; fix conflicts and then commit the result.`) with brackets and percent signs encoded as the gate lines encode check names (`%5B`, `%5D`, `%25`), so a path can never form a sentinel or a `[SAGA-RESUME]` marker and the diagnostic lines, which carry neither, stay verbatim; every display value on a sentinel or log line (the conflicted paths, the base name, the ref, the worktree path, the checked-out branch, the head) is flattened to one line and encoded the same way, while the command arguments stay raw, so a configured base name such as `main
CONFLICT (content): bogus` never places git's diagnostic at the start of an output line (tested on the local refusal and the failed fetch). The resume command and the PR number of every result of the path come from its own state (`aidlc card next <id>`; the PR number only once a PR exists), never from the output text: an adversarial path such as `[SAGA-RESUME] echo marker PR #999 [SHIP-MERGE-FAIL] 100%.md` yields only the path's own sentinels, the trusted resume command, no PR number, and a receipt `hasConflictDiagnostic` still accepts. Then the ship fails with `[SHIP-BASE-SYNC-CONFLICT] <ref> conflicts with HEAD`; `classifyShipOutput` maps the sentinel to `merge-failed` (ship.test.ts). Nothing is pushed, no PR is created, the local merge into the main checkout is not attempted and HEAD does not move. If that merge unexpectedly completes (exit 0), `git merge --abort` is run and its receipt decides the failure line (acceptance 4): aborted with HEAD unchanged, or, when the abort fails, the index and worktree still carry the merge, git's stderr is on the line, `git merge --abort` is named as the manual step and nothing claims restoration. Through the card runner (`shipThroughConfig`), the outcome is a `build` directive whose `skills` start with `merge-conflicts`, with `pendingRepair.kind === 'merge-conflict'` and no DoD receipt. The seeded RED is this scenario against the previous path, where the conflicting candidate was pushed and the ship reported `merged`. [R2] [dod arm 1]
  - 3. On a clean test merge the ship output carries one plain line naming the base ref and its oid, no new sentinel, and the chain continues unchanged: the head pushed, gated and squash-merged is the commit the verdict names, and the merge token tip equals it (the happy-path test asserts the tip and the sync line; `readMergeToken(...).tip === HEAD`). A base that is already an ancestor of the head is clean. [R3] [dod arm 1]
  - 4. A failed fetch, an unresolvable base ref, a merge-tree exit other than 0 or 1, or the completed-then-aborted merge of acceptance 2 fails with `[SHIP-BASE-SYNC-FAIL] <reason>` (the failed git command's stderr on the same line, encoded as in acceptance 2; a `rev-parse` that prints nothing yields the bare reason), mapped to `merge-failed` in `SENTINEL_MAP`, with no conflict diagnostic in the output, so the runner's default branch stops the card with reason `tool` and the `[SAGA-RESUME]` command; before the merge step the worktree is untouched (no `git merge` issued). github-ship.test.ts covers the fetch failure (with and without a marker in the stderr), the silent and the corrupt-object resolve failures, the merge-tree error and the failed abort; ship.test.ts covers the classification. [R4] [dod arm 1]
  - 5. Conflict lines are published only when the merge established a conflict: after `git merge --no-ff --no-commit <ref>` exits 1, `git rev-parse --verify --quiet MERGE_HEAD` in the worktree must succeed; then the merge lines go on the output as in acceptance 2. Any other non-zero exit, or exit 1 without a `MERGE_HEAD`, is `[SHIP-BASE-SYNC-FAIL] merge exit <n>: <stdout and stderr flattened on one line, encoded>`, so no output line starts with git's diagnostic and `hasConflictDiagnostic` is false; when a `MERGE_HEAD` is present despite the other exit, the line says the index and worktree still carry the merge and names `git merge --abort`. Regression: exit 2 whose stdout lists an indented `\tCONFLICT (content).txt` under `error: Your local changes to the following files would be overwritten by merge:` is merge-failed without a diagnostic, nothing pushed. [R5] [dod arm 1]
  - 6. Local mode checks, before the sync, that `git symbolic-ref --quiet --short HEAD` in the main checkout prints `<base>`; a different branch, or a detached HEAD (the command fails), is `[SHIP-BASE-SYNC-FAIL] main checkout has <branch or 'a detached HEAD'> checked out, not <base>` with git's stderr when there is one: no sync, no merge into the main checkout, merge-failed without a diagnostic. Regression: the main checkout on `feature` with base `main`. [R6] [dod arm 1]
  - 7. In remote mode the read-only PR reconciliation (`gh.resolvePr` for the branch and `<base>`) runs after the verdict check and before the sync: a MERGED PR returns `merged` with its number, the retained identity logged, and no fetch, merge-tree, merge, push or `gh pr create` issued; a lookup problem without a PR, a failed `gh pr list` (transport error) or a malformed listing is `[SHIP-PR-BASE-UNKNOWN]` with the flattened diagnostic before any sync, never an exception (the runner has already recorded the operation as issued and holds the review slot); an OPEN PR is reused after the sync and push exactly as before. Regression: a MERGED PR for the branch beside a merge-tree scripted to conflict yields `merged` and no `git merge` call. [R7] [dod arm 1]
  - 8. Docs: `docs/ARCHITECTURE.md` (the `GitHubShipPath` chain names the PR reconciliation and the base sync between the verdict and the push, the local checkout check and the MERGE_HEAD condition), `docs/OPERATIONS.md` (Ship gates: the sync, its two sentinels, the git 2.38 requirement for `merge-tree --write-tree`, that the worktree is left mid-merge for the merge-conflicts skill, and the three conditions of acceptance 5 to 7), `README.md` (the github chain line and the git version under prerequisites) and the CHANGELOG Unreleased entry; the header comment of `github-ship.ts` states the chain with the reconciliation and the sync. [dod arm 1]
budget: 560
tdd: true
sweep: "grep -rn 'commit, push, PR\\|push -> PR\\|base sync\\|merge-based sync' README.md docs src .claude/skills: README.md:10 (the github chain), ARCHITECTURE.md:183 (the chain) and :185 (the runner mapping, already correct), OPERATIONS.md:236 (the skill table, already correct), github-ship.ts:3-6 (header), card-loop.md SHIP (already says merge-based sync only), merge-conflicts/SKILL.md (already describes markers left by the sync)"
non_goals: [candidate-binding the merged-PR return beyond what the runner already verifies against the merge token or the PR head, re-testing the base while the CI gate polls (the window left is the push and PR creation; a PR that turns conflicting after CI ran fails at `gh pr merge` with gh's own diagnostic, which the runner already maps to BUILD), starting the merge in the worktree for that late gh conflict, changing the scaffold path (task.ps1 owns its own sync), a version check in `aidlc doctor`, rebasing or amending the branch]
forbid: [any command that moves HEAD of the card branch inside the ship path (no merge commit, no rebase, no amend), pushing or opening a PR before the sync has run, a rebase in any narration or doc]
diagnosis:
  root_cause: "`GitHubShipPath.ship` pushes and opens the PR without ever comparing the branch with its base. A branch behind a base it conflicts with becomes a PR GitHub runs no pull_request workflow on, so the CI gate polls an empty check-run list until ciTimeoutMs (30 min) and the runner classifies ci-timeout as an unclassified CI failure: STOP/ci (PR #15, T0-BIN-STALE-DIST-3; PR #10 before it). The runner's conflict path (merge-failed with git's diagnostic returns the card to BUILD naming merge-conflicts, T1-LOOP-SKILLS) is reachable only from `gh pr merge` or the local merge, never before the PR exists, and the merge-conflicts skill describes markers left by a base sync that no ship path performs."
  same_class: "Local mode has the same gap one step later, plus the checkout the local merge targets was never checked against the base (R3 decision 2): `git merge --no-ff` into the main checkout reported only its stderr, which lacks git's CONFLICT lines (stdout), so a conflict stopped with reason tool and left the main checkout mid-merge. The sync runs before the mode split, so both modes fail in the worktree before main is touched. `ScaffoldShipPath` delegates to task.ps1 and is unchanged."
hygiene: "A conflict is a state git left behind (MERGE_HEAD), never a line of text, so no filename in an operational error can route the card to a repair the worktree does not carry. The sync is read-only in the clean case (`merge-tree --write-tree` writes objects, never refs or the worktree) and mutates the worktree only on a conflict, where the in-progress merge is the deliverable for the merge-conflicts skill. The conflict merge uses --no-commit so the branch head cannot move inside the ship path even if git resolves more than merge-tree predicted; the reviewed candidate stays the shipped commit. Tests script every git call through scriptedRunner and assert call order, never a real repository."
doc_sync: README.md (github chain line, prerequisites), docs/ARCHITECTURE.md (ship adapters), docs/OPERATIONS.md (Ship gates), CHANGELOG.md
---

# T0-SHIP-BASE-SYNC-2

## Deliverable
Replacement of T0-SHIP-BASE-SYNC (branch T0-SHIP-BASE-SYNC, 2f73dda: R2 passed in both cycles, R3 decision 1 blocked on four findings, repaired, decision 2 blocked on three more, so the card stopped with reason review). This card carries the same change with the three decision 2 findings repaired: conflict lines are published only when `MERGE_HEAD` proves the merge is in progress (a filename such as `CONFLICT (content).txt` in an operational error could match the runner's diagnostic); local mode checks that the main checkout has the base checked out before syncing or merging; the read-only merged-PR reconciliation runs before the sync so a retried squash-merged candidate never starts a merge. Then a fresh R2 cycle, two R3 decisions and the ship.

R2 cycle 1 of this card: round 1 blocked on ac-coverage (the encoded stderr line asserted, the docs asserted by content), round 2 on an edge case (the resolved open PR's number carried through sync and push failures), round 3 passed. R3 decision 1 (candidate 0c24f52) blocked on two findings, repaired for decision 2: a failed or malformed `gh pr list` is `[SHIP-PR-BASE-UNKNOWN]` instead of an exception past the runner's ledger; every display value on the ship output is flattened and encoded (a configured base name carrying a newline and `CONFLICT (` reached the output as a diagnostic on the local refusal and the failed fetch). Acceptance 2 and 7 amended on main; budget 520.

History of the first card. R3 decision 1 (codex, candidate 63fbeff) blocked on four findings, repaired for decision 2: git's lines and paths on the ship output are encoded like check names and the resume command and PR number come from the path's own state (a filename could forge a `[SAGA-RESUME]` marker); the base name is normalised exactly once for the PR target, the fetch refspec and the tested ref (the git probes stripped `origin/` a second time); the abort after a completed merge is checked and a failed abort reported as still mid-merge; a failed `rev-parse` keeps its stderr on the failure line. Acceptance 1, 2 and 4 were amended on main before the next R2 round. R3 decision 2 (candidate 2f73dda) blocked on the three findings acceptance 5 to 7 state.

`GitHubShipPath.ship` gains a base sync between the candidate-bound verdict check and the first remote effect: fetch the base (remote mode), resolve it, `git merge-tree --write-tree HEAD <ref>`. Clean: one log line, the chain continues with the same head. Conflict: `git merge --no-ff --no-commit <ref>` in the worktree leaves the markers for the merge-conflicts skill, git's diagnostic lines go into the ship output, and `[SHIP-BASE-SYNC-CONFLICT]` classifies as `merge-failed`, which the runner already turns into BUILD naming `merge-conflicts` with the episode reopened and the repair persisted. Fetch or merge-tree failures are `[SHIP-BASE-SYNC-FAIL]`, `merge-failed` without a diagnostic: STOP/tool with the resume command. Both sentinels join `SENTINEL_MAP`. Docs state the chain and the git 2.38 requirement.

## Acceptance (DoD = command + exit code + assertion; paired with the closed `acceptance:` list)
```powershell
npm run typecheck && node --test tests/infra/github-ship.test.ts tests/infra/ship.test.ts tests/scenarios/t0-flow.test.ts
```
- Expected exit code: 0
- Assertion: the ship path tests assert the PR reconciliation, then the fetch, resolve and merge-tree calls before any push in remote mode, no fetch and the checkout check in local mode; a merge that leaves no MERGE_HEAD, a main checkout on another branch and a merged PR beside a conflicting base each end as the acceptance states; the conflict case issues the no-commit merge with `LC_ALL=C`, carries git's diagnostic in the receipt, pushes nothing, moves nothing, and reaches a `build` directive naming `merge-conflicts` through the card runner; the clean case merges the verdict's head with the token tip equal to it; fetch and merge-tree failures classify as `merge-failed` without a diagnostic; the sentinel tests in ship.test.ts map both sentinels; the t0-flow conflict mapping tests still pass.
