# References

Sources that shaped this implementation, with the revision or date actually consulted. They inform the design; their existence does not prove this integration works (plan v5 §10).

| Source | Pinned revision / date | What was taken | Where it landed |
|---|---|---|---|
| Anthropic, "The AI-native SDLC playbook" (claude.com/blog/the-ai-native-sdlc-playbook) | fetched 2026-09-10 | Six-stage loop and committed artifact chain (intent.md, spec.md, plan.md, diff, REVIEW.md, bands.yaml), skills-vs-hooks hierarchy, verifier subagent, production-gate hook, managed-settings example, continuous evals, control-band monitoring tiers | `src/artifacts/*`, `src/hooks/index.ts`, `src/maintain/*`, `src/evals/runner.ts`, `templates/*` |
| Anthropic, "Effective harnesses for long-running agents" | via SDLC-CAPABILITY-REVIEW (2026-09-10) | Explicit feature list, per-item progress, real end-to-end verification before declaring done | goal DONE predicates in `src/core/goal-machine.ts`, `verify-arc` directive |
| claude-devops-scaffold (D:\Projects\claude-devops-scaffold) | `cecf7f63` (v0.47.0), read 2026-09-10 | Card schema and sentinels, verdict schema (two axes, run_status, routed_skip), task.ps1 ship chain and sentinels, review round/verdict files, merge token, hook protocol, settings.json deny list, QUALITY-RUBRIC dimensions, execution hard limits | `src/artifacts/card.ts`, `src/core/review-policy.ts`, `src/delivery/ship.ts`, `templates/REVIEW.md`, `templates/claude/settings.json`, `templates/CLAUDE.aidlc.md` |
| MyInspection (downstream, D:\Projects\MyInspection) | `e56b00fd`, read 2026-09-10 | Downstream deltas: live `-SkipRed`, RED receipt requirement, `.rounds` cap 2, watershed receipt, TASK-BOARD format | `ScaffoldShipPath.readRedReceipt/readVerdict`, board view |
| AWS `awslabs/aidlc-workflows` | `aadc832354f2d8841489ed01b5e0d852b32b7962` (v2.8.x line), fetched 2026-09-10 | One typed directive per call, read/write split (`next` vs `report`), six-state checkboxes, per-clone audit shards, scope precedence, park/resume | `src/loop/directive.ts`, `src/loop/controller.ts`, `src/state/board.ts` |
| `fabriqaai/specs.md` (FIRE flow) | `c9c2ba8e80b809787d80a202ee394e1c5847dddf` (v0.1.74), fetched 2026-09-10 | State file as the single truth, resume by persisted phase (never by artifact existence), namespaced error codes, artifact validator shape | `src/state/goal-store.ts`, `src/core/card-machine.ts` precedence, `src/artifacts/*` validators |
| `ai-sdlc-framework/ai-sdlc` | `a739ceceb3d68522404816bb21372cbb32da43b4` (plugin v0.20.1), fetched 2026-09-10 | Injectable adapter bag, in-flight claim with sentinel recovery, explicit outcome sets, Definition-of-Ready gates as pure functions, CI cooldown, content-addressed attestation | `src/loop/*` dependency injection, `src/artifacts/plan.ts` (`evaluatePlanReadiness`), `src/audit/manifest.ts` |
| IBM Bob docs, IBM DevOps Deploy | via SDLC-CAPABILITY-REVIEW (2026-09-10) | Explicit context per subtask, reviewable plans, per-task authorization, distinction between workspace rollback and real deployment recovery | role profiles (`src/core/roles.ts`), release recovery semantics (`src/core/release-machine.ts`) |
| Microsoft "Safe deployment practices" | via SDLC-CAPABILITY-REVIEW (2026-09-10) | Health measured over a declared window, staged exposure, rehearsed rollback | `src/core/health.ts`, `src/loop/release-runner.ts` |
| Anthropic Claude API docs (claude-api skill, cached 2026-06-24) | read 2026-09-11 | `@anthropic-ai/sdk` usage: adaptive thinking, `output_config.effort`, streaming `finalMessage()`, typed errors, refusal stop reason | `src/providers/claude-api.ts` |

Working notes with verbatim extracts live in the session's `findings.md` (gitignored); the plan documents under `docs/plans/` remain the requirement authority.
