# Changelog

## Unreleased

- Base-sync hold narration, card T0-BASE-SYNC-HOLD-NARRATION: a quota hold of the base-sync reviewer now makes `aidlc card next` wait with a narration that names the base-sync reviewer as held; with a fallback configured it used to say that the primary and the fallback both reported a quota, which was false.

- Ship failure that refutes nothing, card T0-SHIP-NOTHING-REFUTED: a ship failure that refutes no recorded success (a run with no effort episode, or one whose last evaluated attempt is not a success) no longer says the failure counts as a failed attempt; its build directive says it counts no attempt. The build directive of a ship failure names the ladder's attempt number, where it used to count not-counted attempts and name a number the next build directive did not.
- Running repair on a ladder stop, card T0-RUNNING-REPAIR-STOP: a repair attempt still running when a ship failure stops the effort ladder can no longer be recorded; `aidlc card attempt` refuses it with any outcome and names the stop, where a success used to overwrite the stop with `succeeded`. A running repair the ladder promotes to the escalation is journaled as an `ATTEMPT_STARTED` with `promoted: true`, so the journal no longer shows it as a baseline attempt.

- Formal review on Codex gpt-6-astra, card T0-R3-CODEX-ASTRA: this repository runs R3 on Codex `gpt-6-astra` in a read-only sandbox at `medium` for every candidate, with the headless Claude Opus 5.5 reviewer, its read-only flags and its effort policy moved to `formalReview.fallback`, which runs while Codex is held on quota. `gpt-6-sol` is refused for a Codex login with a ChatGPT account. The base-sync reviewer keeps its command under the name `codex-base-sync`, since the configuration refuses a base-sync reviewer named like the primary.

- Ship repair attempt, card T0-SHIP-REPAIR-ATTEMPT: a ship that fails on the candidate's own code (a CI failure classified as a code defect, `dod-failed`, `verify-failed`, `scope-blocked` or `budget-over`) now counts the attempt that bound the candidate as a failed attempt, with the ship outcome as its cause, and reopens the effort episode, so `aidlc card attempt` records the repair; the card used to return to BUILD with the episode still succeeded, `aidlc card next` kept asking for the next attempt and `aidlc card attempt` refused it (`episode already succeeded; ship the candidate instead`) until the card deadline stopped the card. Repeated failures follow the effort ladder to STOP/card; a base-sync conflict, a rejected RED receipt, a review block and a transient CI failure keep their rules (docs/OPERATIONS.md).

- Base-sync decision, card T0-BASE-SYNC-REVIEW: a candidate made by resolving a base-sync conflict after both R3 decisions are used now gets one more decision by `formalReview.baseSync` (this repository: Codex `gpt-6-astra` at medium effort) instead of stopping for review; its pass ships and its block stops the card. The decision is taken once per base-sync candidate, its prompt says the base moved, and every other candidate past the two-decision allowance stops as before (docs/OPERATIONS.md).

- Unattended runs, card T0-UNATTENDED-RUNS: `docs/OPERATIONS.md` documents a `/goal` condition that drives a goal through `aidlc next` and `aidlc report` until the latest directive is `done`, `stop`, `ask`, `checkpoint` or `wait`, and a self-paced `/loop` that waits on a `wait` directive by its `until` or `pollSeconds`; a `/schedule` cloud routine cannot drive a goal, since `.aidlc/` is local and gitignored. The condition tells the agent not to act on or report those five kinds, so no goal turn approves a checkpoint or answers an ask. Every `--dod-receipt` example in `docs/OPERATIONS.md` and `README.md` names the test count, since `node --test` exits 0 on a file with no test, on a file whose every test is skipped and on a glob that matches no file. Documentation only; the loop's behaviour is unchanged.

- Base-sync CHANGELOG merge, card T0-BASE-SYNC-CHANGELOG completed as T0-BASE-SYNC-CHANGELOG-2 (the replacement after a wrong full-check claim on the first candidate): a base-sync conflict whose only unmerged path is `CHANGELOG.md` and whose every hunk is lines both sides added to the Unreleased section is now merged by keeping both, the card's lines first, and the ship ends with `[SHIP-BASE-SYNC-MERGED]` instead of `[SHIP-BASE-SYNC-CONFLICT]`; every other conflict is left to the merge-conflicts skill as before. The merge commit is a new candidate that goes through the DoD, R2 and R3; the ship never pushes it on the reviews of the candidate it replaces (docs/OPERATIONS.md).

- Ship-path pool hold by outcome, card T0-SHIP-HOLD-OUTCOME: only a `review-no-verdict` ship outcome holds the review pool on a quota message; a ship that merged, whose CI gate failed or that ended with any other outcome now settles its pool request without a hold, sets no pool `resetAt` and journals no `REVIEW_HOLD`, whatever quota message its receipt carries. A merged ship with `429 Too Many Requests` on stderr used to hold the pool for 15 minutes and leave its request in `retry-after`; the pool then admitted that request of a closed card as running, so in a pool with one slot no other card was admitted to ship (docs/OPERATIONS.md).

- Approver identity, card T0-APPROVER-IDENTITY: `aidlc authorize`, `aidlc plan approve` and `aidlc report --result approved` run without `--by` now record the git identity of the repository as `grantedBy` (`git config user.email`, else `user.name`, through the new `GitProbe.userIdentity`), where they recorded the literal `user`, so the goal record names who approved; `user` stays when neither is set. An explicit `--by` is recorded as given, an empty one included, which the record refuses (as `authorize` and `plan approve` already did), and a `by` key in `--data` is passed on as it is. No other command reads the git identity, and the controller's own fallback for library callers is unchanged (docs/OPERATIONS.md).

- Audit re-admission, card T0-AUDIT-READMIT: `aidlc audit verify` no longer reports `WORK_AFTER_TERMINAL` for the work of a goal the user re-admitted with `aidlc goal resume` or with `aidlc goal extend` after a time stop, which dropped such a goal to `recorded`; it counts a dispatch, an issued operation or an attempt only when the latest disposition before it is `GOAL_DONE` or `GOAL_STOPPED`. Work after the final disposition, work of a generation below the highest one a resume moved to and work after a lease takeover still block. Of the goals in this repository's state when this card ran, the four that were resumed (g-20260915193112-db0472, g-20260917214550-c76e7f, g-20260918021545-195e85 and g-20260925014420-bcf1ef) reported 4, 4, 5 and 4 such events and now report none; every other goal reports what it reported before (docs/OPERATIONS.md).

- Ship-path quota hold, card T0-SHIP-QUOTA-WAIT completed as T0-SHIP-QUOTA-WAIT-3 (the second replacement, after the R2 rounds of T0-SHIP-QUOTA-WAIT and T0-SHIP-QUOTA-WAIT-2 ended on verdict lines inside unclosed code fences): a `review-no-verdict` ship outcome whose receipt carries a quota message now returns a `wait` directive on `review-quota` until the 15-minute review-pool hold passes, and the next `aidlc card next` after it ships the same candidate again; the card used to stop with STOP/review and `missing/malformed/stale verdict after the single retry`, although the pool was held and no retry or decision had been spent. A `review-no-verdict` ship outcome without a quota message still takes the single retry and then STOP/review (docs/OPERATIONS.md).

- Pre-review answer marker, card T0-R2-ANSWER-MARKER: `preReview.answerMarker` (default empty, which reads as before) makes the pre-review reader read each angle only after the last stdout line equal to the marker, and makes an output with no marker line a `malformed` no-verdict round; this repository sets `=== answer ===`. Four DeepSeek angle outputs on T0-SHIP-QUOTA-WAIT and T0-SHIP-QUOTA-WAIT-2 ended on a pass verdict line and were read as no verdict, since their reasoning quotes diffs with a closing fence line and no opener and so left a fence open to the end of the output; every fence line was before `=== answer ===` (docs/OPERATIONS.md). The formal review (R3) reader is unchanged.

- Quota holds, card T0-QUOTA-FALSE-HOLD completed as T0-QUOTA-FALSE-HOLD-2 (the replacement after two R2 rounds lost to DeepSeek CLI read timeouts): a reviewer that exits 0 without a readable verdict is now a no-verdict round when a quota word appears only on its stdout, where it was a quota hold; a process that exits 0 is held only on a quota message in its stderr, and any other process on a message in its stdout or stderr, as before. The ship path reads the receipt of its ship command by the same rule. The quota patterns of `detectQuotaHold` match whole words only: a word ends at any character other than a letter or digit and at a camelCase change of case, and `_` separates the words of a phrase, so `Quotation`, `4290`, `HTTP429`, `E429`, a sha containing `429` or a one-case compound such as `quotaexceeded` never hold, while `quotas`, `429s`, `rate-limited`, `insufficient_quota`, `rateLimitExceeded`, `RateLimitError` and `APIQuotaExceeded` still do. The camelCase and `_` forms of the phrases, such as `retryAfter`, `tooManyRequests`, `usageLimit`, `rate_limit` and `too_many_requests`, now hold, which no pattern matched before. On T1-REVIEW-LOOP-GUARDS an R2 angle that exited 0 with a verdict cut before its last brace and `Quotation marks` in its reasoning was held as a quota hold instead of taking the no-verdict retry (docs/OPERATIONS.md).

- Formal review on Opus 5.5, card T0-R3-OPUS-PRIMARY: Codex is unavailable, so this repository runs R3 on the headless Claude Opus 5.5 reviewer that was its fallback, with the same read-only flags, effort policy and timeout, and with no fallback, since a Codex fallback would be dispatched on an Opus quota hold and fail. `docs/OPERATIONS.md` keeps the Codex `gpt-6-sol` command as the way to restore it, and `CLAUDE.md` names Opus 5.5 for R3.

- End-of-turn wording and removed-instruction patterns, card T1-PROMPT-CHECK-2: the end-of-turn line of every R2 and R3 prompt and the end-of-turn rule in `docs/OPERATIONS.md` now say that a note after the verdict line is ignored only when it contains no JSON, since the reader takes the last JSON document that parses; `docs/OPERATIONS.md` said that any note after the verdict line is ignored. `removedInstructions` (`tests/surface/prose.test.ts`) also catches "be very conservative", "be more conservative", "stay more conservative", "re verify" and "verify ... with a subagent" with any number of words between, and "subagent" spelled "sub-agent" or "sub agent", each with a self-test case. A "verify" and a "with a subagent" in two sentences, paragraphs or list items no longer match, where the old pattern matched across a sentence end within three words; a sentence ends at `.`, `!` or `?`, after any closing quote, bracket, backtick or emphasis mark, followed by whitespace and a capital letter; the dot of an ellipsis or of e.g., i.e., etc., vs., cf., Mr., Mrs., Ms. or Dr. ends none, and any other abbreviation before a lowercase word stays inside the sentence.

- Rename-aware changed paths, card T1-RENAME-PATHS: the changed-path list of R2 and R3 (`collectCandidateDiff`) and `GitProbe.changedPaths` run `git diff --name-only -z --no-renames`, so a renamed file is listed by its source and its destination, and a non-ASCII name as written, never C-quoted. The scope gate now refuses a rename from a path outside `allow_paths` into it, which it passed while git named a rename by its destination only. The effort path rule reads the same list, so `renameSources` is removed, and renaming a non-ASCII file out of a single-segment glob such as `src/core/*.ts` now selects `high`, where the octal escapes of the `rename from` line never matched it. Every R2 and R3 prompt lists both paths of a rename among the changed paths (docs/OPERATIONS.md).

- Review loop guards, card T1-REVIEW-LOOP-GUARDS: `aidlc card attempt --outcome success` on a `tdd: true` card whose run and attempt carry no RED receipt is refused with an error naming the missing RED receipt and records nothing, where it used to bind the candidate and leave the card in BUILD with every later attempt refused. A verdict document inside a closed ```json or bare ``` fence is read as the verdict, and a document inside a fence that never closes is malformed. Backticks right after the document close its fence only when nothing but whitespace follows them on that line. The reader already read a closed fence before this card, so the cause of the no-verdict round T1-OPUS55-PROMPTS lost is not established; the new rule only makes an unclosed fence fail closed like a document cut short (docs/OPERATIONS.md).

- Review and agent prompts for Opus 5.5, card T1-OPUS55-PROMPTS: every R2 and R3 prompt and the reviewer agent end the turn only on the verdict JSON line; a progress note, a summary that announces a next step or an offer to continue is not the end of the review, and a reply that ends on one with no verdict line before it has returned no verdict. The R2 prompt asks for every finding the reviewer can defend and says its block rule decides only whether a finding blocks. The implementer agent ends its turn only with the report or a named blocker, and `arc.md` gives a child the elapsed time against its deadline (`elapsed <s> / <s>`). `tests/surface/prose.test.ts` keeps every agent, skill, `REVIEW.md` and review prompt free of the instructions the Opus 5 and 5.5 guides say to remove (double-check or re-verify, "think carefully", "think step by step", "use a subagent to verify", "be conservative", "only report high-severity", each in the word orders and spellings the test lists).

- Claude defaults on Opus 5.5, card T1-OPUS55-MODELS: `DEFAULT_MODELS.claude` names `claude-opus-5-5` for the planner, implementer, reviewer and release-specialist (the investigator stays on `claude-sonnet-5`), the Claude API provider defaults to `claude-opus-5-5`, and the Claude effort ladder is low, medium, high, xhigh, max, so an escalation from `high` goes to `xhigh` and task assessment never starts at `max`. Tests pin that the provider sends the task's effort as `output_config.effort` on every request, `thinking` absent or adaptive, no `temperature`, `top_p`, `top_k` or `tool_choice`, and a last message from the user (Opus 5.5 rejects each of those settings), that a leading empty thinking block is not part of the text, and that `stop_reason: "refusal"` is a refusal. `ClaudeApiOptions.client` injects a messages client for those tests.

- Formal review effort per candidate, card T1-OPUS55-R3-3 (superseding T1-OPUS55-R3 and T1-OPUS55-R3-2, each stopped after two R3 decisions): `formalReview.effort` and `formalReview.fallback.effort` (`default`: `medium`, `high`, `xhigh` or `max`, `medium` when omitted, `low` refused, a `high` rule under an `xhigh` or `max` default refused; an optional `high` rule with a required `high.minChangedLines` and an optional `high.paths`) choose the level a new `{effort}` argv placeholder expands to: `high` when the candidate's added plus deleted lines, counted from the hunks of the pinned `--text` diff collected for the review, reach the threshold or a changed path matches a glob, the default otherwise (`src/core/review-effort.ts`). The level is chosen with the dispatched reviewer's own policy before the first mutation of the dispatch and recorded as `effort` on the `ReviewInvocation`, only when the argv carries `{effort}`. This repository keeps Codex `gpt-6-sol` as the R3 primary, now with `-c model_reasoning_effort={effort}`, and the Claude Opus 5.5 fallback, now with `--effort {effort}` instead of `--effort max`; both run at `medium`, and at `high` from 500 changed lines or a change under `src/core`, `src/coordination` or `src/state`. The template gains `formalReview.effort` `{ "default": "medium" }`. The candidate diff collected for R2 and R3 is taken with `--no-color --no-ext-diff --no-textconv` beside `--text`, so no user git setting colours it or hands it to an external driver or a `textconv` filter; a non-empty collected diff with no `diff --git ` section selects `high`, or the policy default when that is higher, instead of counting zero; the path rule also matches the `rename from` source of each rename; `ReviewEffortLevel` is derived from `EffortLevel` without `low`.

- Fallback formal reviewer, card T0-R3-FALLBACK-2: `formalReview.fallback` names a second R3 reviewer command. While the primary's latest invocation of the card holds an unexpired quota hold (a repaired candidate included), `card next` issues the review directive for the fallback and `review r3` runs it, where the card used to wait in `review-quota` until the hold cleared; with both reviewers held the card waits until the earlier hold clears. Decisions by either reviewer share the two-decision allowance, the no-verdict retry and the same-candidate rule, and the invocation, `REVIEW_DECIDED`, the canonical verdict document and the `review r3` summary name the reviewer that ran. With a fallback configured each formal reviewer queues in its own review pool (`<reviewPool>/<reviewer>`), since a hold resets the pool it lands in, a request already stored keeps its pool, and reserving one reviewer's dispatch cancels, best-effort, the card's leftover request of the other for that candidate, so a held request is never admitted later by another card; without one nothing changes. A fallback argument may not be empty, since the Windows shell drops it. This repository's R3 is now Codex `gpt-6-sol` with a headless Claude Opus 5.5 as the fallback (Read, Grep and Glob only, no setting sources, no MCP servers), and the card-loop rule on quota names the fallback as the only allowed reviewer switch (docs/OPERATIONS.md).

- Worktree root edge cases, card T0-WORKTREE-ROOT-EDGE: `resolveWorktreeRoot` now refuses a main checkout with no directory name (a filesystem root such as `D:\` or `/`) with an error naming the checkout and `worktreeRoot`, where it resolved to the unscoped `%SystemDrive%\wt` or `$HOME/.wt` that T0-WORKTREE-ROOT-DEFAULT retired; a `SystemDrive` or `HOME` set to the empty string counts as missing, so the `C:` and `/tmp` fallbacks apply where `path.join` produced the relative `wt\<name>` or `.wt/<name>`. The signature, every call site and an explicit `worktreeRoot` are unchanged (both were R2 advisory notes on PR #29; docs/OPERATIONS.md).

- Per-repository worktree root, card T0-WORKTREE-ROOT-DEFAULT: an empty `worktreeRoot` now resolves to `%SystemDrive%\wt\<name>` on Windows and `$HOME/.wt/<name>` elsewhere, `<name>` being the base name of the main checkout, instead of the unscoped `%SystemDrive%\wt` or `$HOME/.wt` every repository on the machine shared, so two repositories with a card of the same id no longer collide on one directory. `resolveWorktreeRoot` takes the main root as a required parameter (the ship paths, the card runner's worktree path and PREPARE, and `aidlc cards project` pass it) and applies the named platform's path rules, so its result is the same on any host. An explicit `worktreeRoot` is unchanged. A repository whose card worktrees still sit under the old default is not searched: its next PREPARE stops with the existing ownership message (`worktree for <id> is at ..., expected ...`); set `worktreeRoot` to the old root, or move the worktrees, before running the card again (docs/OPERATIONS.md).

- Coverage statistics, card T1-REVIEW-COVERAGE-STATS: `aidlc review stats` now reports the acceptance coverage T1-REVIEW-COVERAGE-2 retains in shadow, per card and in the family totals. `summarizeReviews` adds `coverage`: `roundsRequested` and `roundsComplete` over the decided rounds that retained a coverage record, the summed `unaccounted`, `conflicted` and `inconsistent` item counts, and `r3SpecFindingsAfterIncomplete`, the formal findings whose reason carries the `[spec]` axis tag and whose candidate's last decided R2 round left an item unaccounted. Every number is read off the round records and the findings the loop persists, never off a verdict or a later classification; the rounds and the findings of every run of the card are joined before they are matched, so a candidate reviewed under one goal and cited under another is read as one timeline, and a candidate stops counting as incomplete once a later round is decided on it. The rounds are ordered by the decision each one landed rather than by its request, since a round held for pool admission is decided after one requested later, and the remaining keys (request, cycle and round, goal, reservation) are all persisted and compared by code unit, so the summary depends on neither the order the runs reached it nor the runtime locale. A round whose decision artifact the run did not retain is placed by its measured end, which cannot cover the wait before the reviewer started, so it never settles a candidate another round states its decision for. `formatReviewStats` prints the segment, or `coverage: not requested` for a card no round of which asked for coverage. This is the measurement a decision on a required coverage mode needs.

- Verdict extraction, card T0-VERDICT-PROSE: an unterminated JSON-looking opener in a reviewer's reasoning no longer voids the complete verdict document that follows it. `topLevelDocuments` now reads JSON from each opener: an output that ends inside one is a document cut short in every state the cut can land in (an open container, a dangling separator or colon, a key without its value, a string cut inside an escape, a literal or a number cut mid-token), so it stays malformed as the T1-REVIEW-FINDINGS-4 rules require, while a character JSON cannot carry there makes the opener prose that opened nothing, and the scan resumes at that character. Each character is read once, so prose full of openers no longer costs a rescan of the output. A prose opener with no parseable document after it still makes the output malformed, so a corrupt final document is never answered by an earlier draft. Three consecutive R2 rounds on T1-REVIEW-COVERAGE had lost seven of nine angle verdicts to this, each angle's last line a well-formed pass.

- Acceptance coverage in shadow, card T1-REVIEW-COVERAGE-2 (replacing T1-REVIEW-COVERAGE, whose R3 allowance was spent): `preReview.coverage` (`off` by default, `shadow` in this repository) asks the `ac-coverage` angle of a pre-review round for one `{item, status, impl, test}` entry per numbered acceptance item; the contract line and that angle's text carry the request, every other angle, the single-pass prompt and the R3 prompt are byte-equal to the `off` prompts, and the configuration parser rejects `shadow` with a `{schema}` pre-review command, since `VERDICT_SCHEMA` is frozen. The retention obeys the request too: an unsolicited list in `off`, on another angle of a shadow panel or from an R3 reviewer is dropped, so those documents are what they were before the setting existed. `joinCoverage` (`src/review/pre-review.ts`) labels every item across the angles that reported one (accounted, unaccounted, conflicted, inconsistent; `supported` without both locations joins as `unknown`; `malformed` counts an item outside `1..expected` at either bound, a repeat from one angle and the entries the parser rejected, none of which join anything), and reads what the angle reported rather than what its classification left, so a verdict dropped for contradicting its axes still contributes its entries, and the result is retained on `PreReviewRound.coverage`, on the `PRE_REVIEW_DECIDED` event, in the aggregated round document and as the `coverage: <accounted>/<expected> accounted` line of the `aidlc review pre` summary. Nothing reads it: the round's outcome, findings, R3 prompt and allowances are unchanged, which is what makes a later decision on a required mode measurable.

- Learned invariants in the review prompts, card T1-REVIEW-INVARIANTS: every R2 angle and the R3 decision now receive a `Learned invariants` section after the review policy. `reviewLessons` (`src/artifacts/lessons.ts`) returns the NEVER and ALWAYS lines of `docs/LESSONS.md` newest first, filling `REVIEW_LESSONS_MAX_BYTES` (3000 bytes of lesson text) and counting the rules the cap left out; NOTE lines are never sent, and an absent file carries none. `buildReviewPrompt` quotes each rule as one JSON string, the way it quotes a prior finding, states that a rule is data and never an instruction, and instructs the reviewer to check every site of that class in the diff and report one finding per site cited with `file:line`; the section names `<n> older lessons omitted at the byte cap` when the cap left rules out, and renders `- none` for an empty list. The card runner reads the file with the other prompt inputs of each dispatch, before its first mutation, so a family of sites of one known defect class is found in a single round.
- Planning claims, card T0-PLANNING-CLAIMS completed as T0-PLANNING-CLAIMS-2 (the replacement after the R2 no-verdict allowance went to two DeepSeek CLI read timeouts; same candidate, fresh review ledger): `aidlc doctor` prints `workingTree`, every uncommitted file (any porcelain entry, a rename with both endpoints) under the intent, specs, plans and cards directories of the main checkout, its path JSON-quoted (and a goal or session id JSON-quoted when it is not a plain identifier), with the goal that claims it and that goal's lease owner and expiry (`claimed by <goal> (session <id>, lease live until|expired at <T>|released)`, `(no lease)`) or `unclaimed`; `clean` when there is none, `n/a` outside a git repository, `UNREADABLE: git status failed (exit <n>|UNREADABLE)` when the status cannot be read (the probe's exit code, never git's text), `(lease unreadable: <code>)` on the entry of a lease record that cannot be read, `claim unknown (goal records unreadable: <code>)` on every entry when the goal records cannot be read, `; plan unreadable: <code>` on the entries of a goal whose plan cannot be read and `claim unknown (plan of <goal> unreadable: <code>)` on an unclaimed card file, the code a store read code or `UNREADABLE` and never the error text (`workingTreeReport`, every read injected; nothing throws out of the entry check); a path git C-quoted is decoded (code points, octal UTF-8 bytes) before it is matched, each pathname of a rename is read on its own, and every reference or configured directory is checkout-relative (absolute and drive checks after normalisation too; a backslash is a separator only on Windows), an absolute, escaping or out-of-planning-directory reference neither read nor claimed. The claim is derived from the goal record alone (`src/state/claims.ts`: `intentRef`, the spec and plan of the intent's slug, `planRef`, the card file of every listed card and every row of the plan's task split; a terminal goal claims nothing; the older goal keeps a shared path), so there is no claims file and no second lock. The Stop hook adds `[aidlc] Planning artifacts of goal <id> are uncommitted on main: <files>. Commit them before the session ends; another session sees only files it does not own.` for each goal whose lease the acting session holds on this host, each file and an unsafe goal id JSON-quoted, in the same message as the DoD context, one goal's unreadable plan isolated to that goal, a card-run record that cannot be read isolated to the DoD check, and a note by code when the planning check cannot run; another session's lease, a released lease, a terminal goal, committed files and a missing state directory add nothing. `aidlc-loop` entry check 1 carries the rule (leave another goal's file alone; commit your own goal's planning artifacts on main as soon as they validate and before you stop). `uncommittedPlanningFiles` reads a porcelain v1 entry whose leading space `GitProbe.status` trimmed (`M path` for ` M path`).
- Review statistics, card T1-REVIEW-STATS: `aidlc review stats [--goal <id>] [--card <id>]` reports what a card spent in review. `summarizeReviews` (`src/review/stats.ts`) is pure over the two review ledgers of the card runs and the registry's `superseded_by` chain: `r2` (decided rounds, blocks with the blocking panel angles, no-verdict rounds, quota holds, the summed round durations), `r3` (the ledger's substantive decisions and blocks, and request to decision summed over the invocations whose decision artifact the run's evidence carries), `findings` (total, pre and formal, the dispositions open, disputed and resolved, the re-raised findings and the first-round misses) and `wallMs` from the first review request to the last pass, or to the last review when none passed; a card run without reviews reports zeros, and a card that supersedes earlier ones also reports each predecessor and the family totals. `--goal` narrows which cards are reported, never what the family is aggregated from, so a predecessor that ran under another goal keeps its numbers; a panel angle named after an `Object` property is counted, not read from the prototype; and a block that never barred the merge (an advisory one) is counted as a block, which the enforcement counter is not. Each review is measured to the completion its artifact records, so the wall time counts a round's wait for pool admission. The command prints JSON when stdout is not a TTY and one line per card otherwise, through `formatReviewStats`.
- Arc dependency gate over prerequisites merged elsewhere, card T0-ARC-EXTERN-DEP: a card whose `depends_on` names a prerequisite merged under a different goal is dispatched instead of stopping the goal with `no admissible work`. `GoalController.cardOutcomes` resolves every `depends_on` id outside the goal's projection against the card registry and records `closed` for a merged one, the rule `computeRevision` already applies when it admits such a projection; a prerequisite that has not merged, or is not in the registry, stays an open gap and the goal still stops. `ArcInput.outcomes` documents that it may carry ids outside `cards`; an id outside the projection is never ordered, dispatched or reported, so the wave, the worker cap and the board rows are unchanged. The resolution is one pure helper, `prerequisitesClosedElsewhere`, which the board view uses too: `renderBoard` takes the outcomes its caller resolved instead of rebuilding them from the projected cards alone (its Arc line read `verdict=stop wave=-` for the projection the controller dispatches), and `aidlc board` prints the text `writeBoard` wrote rather than a second render of its own. The workaround this removes was `aidlc goal resume --cards <prerequisite>,<card>`.
- Review inputs bounded and traceable, card T1-REVIEW-INPUTS: a committed diff above the stage's `maxDiffBytes` is refused before any dispatch, naming the size and the cap (no round, decision, receipt, pool request or journal event; the truncation note is gone); every round, decision, retained verdict document (`policy_hash`) and decision event carries `policyHash`, the sha256 of the applied REVIEW.md text, which the prompt names in its policy heading together with the rule files the candidate changes; a round or decision after an earlier decided one of its stage receives the delta since the last reviewed candidate as its own prompt section (the no-change note for the same commit) and a new finding outside that delta is recorded as a first-round miss (`ReviewFinding.outsideDelta`, marked by `aidlc review findings`); reasons tagged `[question]` or `[suggestion]` are advisory in both stages, kept on the round (`PreReviewRound.advisory`), printed by `aidlc review pre` and handed to the formal prompt as non-blocking notes, and stripped from a ship-path verdict before it is classified (a block carried only by tags is a pass with the notes); a ship-path decision records the `policy_hash` its document names, else the hash of the policy in force at dispatch, and its findings are marked against the delta bound at dispatch; the exhausted hand-off event carries the hash of the round it hands on; `review r3` checks the cap on the diff and the delta before the hand-off and every other mutation; a result envelope naming another policy hash than its reservation is never recovered; a ship-path refusal on advisory notes only stops the card with its receipt kept instead of asking a repair; the pinned diff and delta commands end at the candidate sha; `reviewPolicyVersion` is `REVIEW.md@3` in both configs. The six findings of R3 decision 2 were applied under a human ruling and the card merged without another review cycle; the seventh (an argv pre-review prompt should embed the delta) was disputed: the argv transport omits the diff by design since T0-R2-PANEL, and decision 1 asked for the pinned command.
- Ship-path base sync, card T0-SHIP-BASE-SYNC completed as T0-SHIP-BASE-SYNC-2 (bugfix): `GitHubShipPath` fetches the base (remote mode; local mode first checks that the main checkout has the base checked out, then takes the local branch) and tests the merge with `git merge-tree --write-tree HEAD <ref>` (git 2.38 or newer) after the verdict check and the read-only PR reconciliation (a merged PR for the branch ends the ship as `merged` before any sync) and before any push, PR or local merge. Clean: one log line, the reviewed head ships unchanged. Conflict: the same merge is started in the card worktree with `--no-commit` (markers left for the merge-conflicts skill, the branch head never moves inside the path) and, once `MERGE_HEAD` proves it is in progress (any other outcome is `[SHIP-BASE-SYNC-FAIL]` with the output flattened, so a filename such as `CONFLICT (content).txt` is never a diagnostic), git's own lines go on the ship output under `LC_ALL=C` with brackets and percent signs encoded as the gate lines encode check names (a path never forms a sentinel or a resume marker, every failure detail of the path, verdict reasons and command output included, is flattened and encoded exactly once the same way (the CI gate lines excepted), and the resume command and PR number of every result come from the path's own state, never from the output text; a failed or malformed `gh pr list`, or an entry that does not parse as a PR record, is `[SHIP-PR-BASE-UNKNOWN]`, never an exception; the local checkout is compared by its full symbolic ref), and `[SHIP-BASE-SYNC-CONFLICT]` classifies as `merge-failed`, which the runner already turns into BUILD naming `merge-conflicts`; before, such a branch became a PR GitHub ran no workflow on and the CI gate timed out into STOP/ci (PR #15). The base name is normalised exactly once and names the PR target, the fetch refspec and the tested ref alike. A failed fetch, an unresolvable base, a merge-tree error or a merge that completes although merge-tree reported a conflict (aborted; a failed abort is reported as still mid-merge with git's stderr, never as restored) is `[SHIP-BASE-SYNC-FAIL]` with git's stderr on the line, `merge-failed` without a diagnostic: STOP/tool with the resume command, nothing pushed. Both sentinels are in `SENTINEL_MAP`.
- Card takeover, cards T0-CARD-TAKEOVER and T0-CARD-TAKEOVER-2 (the replacement after two R3 decisions states the guarantees to the store's limits; merged under a human ruling after its own second R3 decision blocked on three findings, every one of them applied before the merge): `aidlc card takeover <card> [--goal <id>]` (`CardRunner.takeover`) takes the card lease of another session once that lease has expired and no operation of the card is unresolved in any goal (the lease is one resource per repository and card): the generation advances through `LeaseStore.takeover`, so a write of the old owner at its generation is fenced; the run, read again once the lease is held so that a stop another process persisted meanwhile stays, records the new `ownerGeneration`, a run interrupted between the lease claim and the PREPARE save (which had none and could not be continued) included; and the card state is selected again from the persisted evidence through the evidence gathering `next` now shares with it, whose renewal revalidates the ownership stop as it does for an owner's own expired lease and keeps any other stop as `card next` would (a deadline the wall clock has passed selects STOP/time). That evidence gathering reads the stored run and never a caller's snapshot, so a dispatch from a window that kept the run it read before the takeover records only its ownership stop on the stored run, writes nothing of its own back, and leaves a persisted blocking stop (risk, time) standing; the one scenario that fed `next` an unpersisted run now persists it. The journal carries `LEASE_ACQUIRED` with `takeover`, `previousOwner` and `previousGeneration`, then `LEASE_RENEWED` with `revalidated`. The record and the ledger are validated inside the reconciliation the store invokes, not on the command's first read: a missing or released lease, a lease this session holds at the generation the run carries, a live lease of another session (expiry alone does not prove the owner stopped) and an unresolved operation (named by id) refuse the takeover before anything is written, and a release, a takeover or an operation that landed meanwhile is seen; the command exits non-zero with the cause. An operation admitted after the reconciliation's read and before the lease write is found right after it: the lease stays taken, the run stays as it was, the command names the ids, and the command run again completes the takeover once they are reconciled. A handoff intent (`NOTE`, `card-takeover-intent`, the previous owner) is journaled before the lease write and one `LEASE_ACQUIRED` per generation after it; a lease this session holds at a generation the run does not carry (a takeover or a claim whose run update did not land) is completed without another advance after the lease is read once more (a record released or taken since the first read refuses without a write), journaled as `LEASE_ACQUIRED` with `completed` and the previous owner recovered from the intent, both resolved by resource and generation across every goal's journal, and refused while an operation of the card is unresolved. The guarantees end where the store's do: `.aidlc/` writes are atomic but have no compare-and-set, so four windows remain, named in `docs/OPERATIONS.md` (Sessions) with their recovery: a renewal by the old owner landing inside the lease store's own read-write window is overwritten (the old owner's next write is fenced); a writer that passed its fence before the lease write and saves after it writes the run at the old generation, which the command run again completes; two completions of one session may both journal the acquisition; and an operation admitted after the takeover's last ledger read is left to the assessment of `card next`. `next` reads the stored run before its plan-checkpoint guard too, and lifts an ownership stop whose lease is absent or released (never one merely expired), so the claim the released-lease refusal points to is reachable; `recordAttempt` and `ciReconcile` save onto the stored run as well, and `recordAttempt` applies no fence, which the documentation says; the plan-checkpoint guard of a T2 goal is asked again after the reconciliation, so a lifted ownership stop never dispatches an unapproved projection; the handoff intent is bound to the acquiring session and the acquisition time, so an intent whose write never landed names no previous owner for a later claim, and an acquisition PREPARE journaled counts as the one, the completion then journaled as such; every hint names the goal the command ran under, and the human line (`--no-json` forces it when piped) follows `completed`. The PREPARE ownership stops name the command with the card and goal ids. The goal lease is untouched; a goal the dispatch stopped on the card's ownership stop is resumed with `aidlc goal resume` after the takeover, and the dispatch then reports the card running until `card next` continues it. The two-window scenario pins the prepared and the unprepared run, the refusals, the interleaved operation, stop, release and same-session takeover, the command, the goal stop, the completion re-read, the cross-goal resolution and the checkpoint guard.
- Bin entries and stale builds, cards T0-BIN-STALE-DIST to T0-BIN-STALE-DIST-4 (replacements after two R3 decisions each and, for the fourth, a CI gate that timed out on a conflicting PR; the source root is enumerated by names so a child that cannot be inspected is a descendant failure, the link tests skip only the platform's refusal, and the spawn regression never passes the test process's own `AIDLC_ENTRY_DEBUG` to the entries): `bin/aidlc.js` and `bin/aidlc-hook.js` load `dist/` only when the compiled entry is at least as new as every file under `src/` (`bin/resolve-entry.js`, reasons `dist`, `no-dist`, `stale-dist` and `src-unreadable`; links followed with cycle detection; an incomplete scan selects the sources; `AIDLC_ENTRY_DEBUG=1` prints the choice). Found when the T1-REVIEW-FINDINGS cards ran every review round through main's stale build instead of the sources under review.
- Session identity (T0-SESSION-IDENTITY, completed as T0-SESSION-IDENTITY-3, bugfix): the loop session is the Claude Code session. `resolveSessionId` reads `CLAUDE_CODE_SESSION_ID`, which Claude Code exports to its Bash and PowerShell subprocesses, after `AIDLC_SESSION` (the older `CLAUDE_SESSION_ID`, which Claude Code never exported, is still read); a hook process acts as the hook event's `session_id`; `verify-before-done` lists only runs whose card lease is absent, released or owned by the acting session, so a Stop in one window no longer demands the DoD receipt of a card another window owns; `aidlc doctor` names the source. Before this every CLI process of every window fell through to `.aidlc/session-default` and lease ownership could not tell windows apart. A lease record the guard cannot read leaves its run listed for every session and is named by card id and store error code only, never by content. `aidlc card status` prints the card lease next to the run (owner session and host, generation, expiry, released, whether this session owns it; an unreadable record by its store error code) and the run's `ownerGeneration`, since a run stopped for ownership before PREPARE carries no owner in its stop text. Transition: a lease claimed under the default token, or before a `/clear`, expires; `aidlc goal takeover` takes the goal lease, and a card lease is continued with `AIDLC_SESSION` set to the owner session id when the run's `ownerGeneration` equals the lease generation, which every completed PREPARE records (a run interrupted inside PREPARE has none and stays stopped; that case and the card takeover command are the card T0-CARD-TAKEOVER above); no lease record is rewritten.
- Review findings with identity and dispositions, cards T1-REVIEW-FINDINGS and T1-REVIEW-FINDINGS-2 (the replacement after two R3 decisions, every finding repaired): every cited reason of a block (R2 round, R3 decision, by command or by the ship path) is a finding `F<n>` on the card run with its stage, round, perspective, cited file and candidate sha; a reviewer re-raises one with `re:F<n>` in the reason (recorded on the prior finding, never a new one); a decided later round of the same stage resolves the findings it did not re-raise. `aidlc review dispute <card> <id> --note` records the author's answer, `aidlc review accept` withdraws it, `aidlc review findings` lists them; the next prompt of either stage renders open findings as ones to verify and disputed findings with the note as ones to re-raise only with new evidence (the `Prior findings` prompt section replaces `Findings to verify`). Same-candidate rule: `review pre`, `review r3` and the SHIP gate refuse a candidate the same stage already blocked while a finding of that block is open, and run on the unchanged candidate once every finding is disputed, reusing the DoD receipt the block cleared (`blockedReceipt`, bound to the candidate digest) so no further attempt is needed; the formal stage keys this on the candidate's own decision (`ReviewInvocation.mergeBlocking`), and a ship-path reviewer (no `formalReview.command`) receives no notes, so its blocks stay pending until the candidate changes. A pre-review pass stays valid for its candidate across cycles, so a disputed R3 block goes straight to decision two. Every cited reason counts, root or axis, advisory blocks included (marked `advisory`); references resolve against the findings that existed before the round; every re-raise reason is kept with its angle (from the structured panel result, a single-angle panel included) and records the index of the dispute it answered, so two distinct answered disputes are a deadlock and two rounds answering one dispute count once; each round is bound to the findings it received at dispatch (`revision` on every finding): a finding changed meanwhile is neither reset nor resolved, only a strictly earlier round's finding is resolved, a re-raise reopens a resolved finding, and a ship-path reviewer delivers an empty snapshot. Both stages key the same-candidate rule on the committed sha whichever reviewer decided, advisory blocks included; a dirty candidate is refused; `review pre` reads the persisted run and reserves its round as `pending` under the lock (the gate waits, an abandoned round is dropped after the timeout and the grace); the residual hand-off to R3 is recorded once per cycle and candidate by the gate and the command alike; a ship-path verdict re-read is the same decision; the DoD receipt a block cleared is consumed on restoration and dropped by a later check failure; `aidlc review findings` reads an existing run and creates none. Prior reasons, notes and re-raise reasons reach the prompt as quoted JSON evidence with an explicit never-instructions statement. Every card-run write goes through the card-run lock (`GoalStore.updateCardRun`, behind `saveCardRun`: an exclusive lock file with a wait whose deadline holds on every retry, a stale age, a takeover serialized by a marker, and an ownership-checked write and release); dispositions write back only the changed finding, review completions recompute their findings against the locked record, every other write merges findings by revision and refuses a stale read that lacks a persisted round, decision or hand-off; a `re:F<n>` reference resolves against the findings the reviewer received; a ship-path decision records its invocation, counters and findings in one locked update, and the residual hand-off marker is persisted before its event is journaled. After R3 decision 1 of the replacement card: the review checkout is probed with `git status` at dispatch (a file edited after the attempt was recorded refuses the review); every guard is re-checked on the record locked at reservation, where the owner's lease is renewed and fenced (a foreign session reserves nothing; a refused R3 reservation cancels its pool request); a completion commits from the locked record in a fixed order (stopped run: evidence only, reservation released, no finding; abandoned reservation: never resurrected; lease renewed then fenced; findings, R3 counters and the action from the locked ledger; a superseded candidate's result kept as history without touching the newer candidate); the gate abandons only a round still pending under the lock and journals only an actual drop; a failed attempt or lost checks drop the retained receipt; a single-angle panel keeps its angle on axis-only findings; the lock takes over a stale lock only when its owner process is gone, checks its deadline before every retry acquisition and propagates stat failures other than a vanished file; `saveCardRun` (the controller's writes included) refuses a snapshot that lacks, un-decides or regresses a persisted ledger entry (`staleLedger`). After R2 round 1 of cycle 1: a snapshot that still holds a reservation the persisted record released is refused too (no resurrection of a pending entry); a ship outcome is applied to the record as persisted, and a candidate replaced or a run stopped while the ship was in flight keeps the outcome as history; a lock this process cannot read is never taken over or released (the read error propagates); a revalidated ownership stop is persisted with the re-derived state before any action. After R2 round 2: the reservation of a round or a decision verifies the candidate identity on the locked record, so a candidate recorded between the command's first read and its reservation refuses it. Card T1-REVIEW-FINDINGS-3 (the replacement after decision 2 of card 2, 8 findings): `CardRun.revision` is bumped by every write under the card-run lock and `saveCardRun` is a compare-and-set on it (a snapshot read before another write landed is refused whatever it changes; runs written before the field read at 0); the ship outcome is applied to the record locked at that moment with the candidate and the stop re-validated there; one pre-review round and one formal review in flight per card whatever the candidate, the round in flight counted in the numbering and the decision in flight in the allowance; a failed attempt or lost checks clear the active DoD receipt too; the checkout is re-probed (status and HEAD) inside the reservation right before dispatch; `reviewBlockPending` keys on the candidate's latest decided invocation; `extractVerdict` treats a second document started after a complete one on the decisive line as malformed and lets the last complete document on the line decide. The disposition journal-delivery finding is a non_goal (the class excluded as finding 19). After R3 decision 1 of card 3: the verdict line is parsed by a string-aware walk over its top-level documents (a document cut short is malformed whatever a nested axis says); a success that reports lost checks is refused; the ship's final transaction keeps the locked ledger and computes the action, the retained receipt and the effort from the locked record; the run the SHIP gate reloads is re-validated (stop, candidate, evidence) before any further write or dispatch; a formal result whose commit lost the card-run lock is committed later from its retained verdict under the same invocation, with the snapshot the reviewer received retained as `.review/<stem>.reservation.json`. After R2 cycle 1 round 1: a second dispute needs a re-raise from a round that received the previous dispute (`sawDisputes`), and a formal dispatch that fails before any receipt retains the failure (`<stem>.failed.json`), cancels its pool request and has its reservation released by the next command when the release was refused. After R2 cycle 1 round 2: the classified result is retained at completion (`<stem>.result.json`), so a retained hold is committed as a hold, and a retained result is committed whatever the current candidate (history for a replaced one). After R2 cycle 1 round 3: the record is re-read under the lock right before the ship is dispatched (candidate identity, receipts, stop, reservations, owner generation), and a drift cancels the intent and the pool request instead of shipping. Card T1-REVIEW-FINDINGS-4 (the replacement after decision 2 of card 3, 11 findings): the verdict is the last top-level document of the whole output (a document spanning lines is one document; a cut-short or malformed final document, or an unfinished enclosing one, is malformed); a stage resolves a finding it raised or re-raised; a second dispute is admitted by any re-raise since the latest dispute that received it; a formal result is recovered only from its atomically published envelope (`<stem>.result.json`), the recovery settles the pool request, a retention failure after the reviewer ran or an old log without an envelope is charged as a no-verdict, a completion of an already-decided invocation removes only a pending reservation, the pre-dispatch re-read cancels the intent and the pool admission when its lock fails, and the gate sends the caller back to `card next` when its reload shows another candidate or cycle; the artifact re-read scenario exercises a second ship and a second read. After R2 round 1 of card 4: the verdict walk is one string-aware pass, so a brace inside a quoted reason of an earlier draft opens no document. After R3 decision 1 of card 4: a final lone brace is a document cut short and whitespace before the first key is unbounded; an envelope is recovered only when complete and bound to its reservation, its outcome authoritative, a recovered hold keeping its recorded deadline; a reservation with no artefact expires from its request into a charged no-verdict with its pool request settled; the gate re-validates after the hand-off reload; the canonical verdict file is published atomically and repaired from the committed invocation's retained verdict before the ship; the transient-CI rerun is decided on the locked CI ledger; the pre-dispatch cleanup attempts every step and keeps the original error. After R2 cycle 1 round 1 of card 4: the envelope carries the verdict it was classified from, so a recovery never reads the sidecar and an inconsistent envelope recovers nothing; the drift cleanup attempts every step too; the review diff cap is 800 kB (`maxDiffBytes`), since the branch diff outgrew the 300 kB default and the reviewers blocked on tests they could not see. After R2 cycle 1 round 2: a failed R2 or R3 dispatch retains its failure best-effort, always attempts the release and the pool cancellation, and rethrows its own error; a round whose dispatch failed before any receipt is dropped by the gate at once. After R2 cycle 1 round 3: a no-verdict or hold envelope that carries a verdict is inconsistent and recovers nothing. After R3 decision 2 of card 4 (17 findings, every one applied under a human ruling with a failing test first and the candidate merged without another review cycle): an enclosing array is a top-level document of the verdict walk (unfinished, cut short; finished, not a verdict), never a container to extract from; the panel settles every angle before it rejects, so no reviewer of the round is running when its reservation is dropped; a release removes only a reservation still pending on the locked record and journals only what it released; the pre-dispatch re-read of the ship recomputes the review ledger counters, the pre-review eligibility and the formal admission; the transient-CI grant, denial and directive are decided inside the locked transaction; the canonical `.review/<card>.json` names the decision it publishes (`invocationId`), a same-sha document that is not this decision's pass is stale, the repair rebuilds it from the committed envelope bound to the invocation and agreeing with its outcome (a sidecar only for a decision from before envelopes existed, and only when it agrees), for a pass and an advisory block alike, and journals a `NOTE` per repair; `REVIEW_DECIDED` is journaled whether or not the publication landed (`canonicalPublished`, `publicationError`); an envelope is complete only when bound to the reservation (invocation, digest, pinned sha) and consistent (pool key, snapshot, a run status of the enum agreeing with the outcome); the envelope and the failure marker carry the admitted queue sequence, and a recovery settles only that request (a request under the reusable key with another sequence is a later attempt's), re-holds a `retry-after` request whose slot release or pool deadline did not land, derives the key of a reservation without an envelope from the reservation's own base, policy version and reviewer, and runs before the stop guard (evidence only on a stopped run, its reservation and pool request settled); a dispatch that fails before any receipt cancels its pool request before releasing the reservation and keeps both when the cancellation fails, the next `review r3` cancelling the request from the marker before it releases and dispatches; the artifact re-read scenario replays one captured ship result through `applyShipResult` under the same and a later operation. `extractVerdict` lets the last JSON-looking line decide alone: a document cut short is malformed, never replaced by a draft from the reasoning (found when an R2 angle's final document lost a brace and its placeholder draft blocked the round). A routed skip decides nothing about the findings. One dispute per re-raise (a withdrawn one included); two rounds of mutual non-acceptance are a deadlock: a third dispute is refused and the STOP detail (R3 second block, exhausted R2 rounds) or the residual handed to R3 (journal and the R3 prompt line) names it. The SHIP gate's hand-back of a blocked, unchanged candidate reopens the effort episode like the review commands do, and exhausted R2 rounds decide before a pending block is handed back. Journal: `FINDING_DISPUTED`, `FINDING_ACCEPTED`; `PRE_REVIEW_DECIDED` and `REVIEW_DECIDED` carry the finding id lists, the exhausted and discarded events included. From the V2 PR-review workflow notes and this repository's own review history (one questionable R2 finding cost two rounds; a second R3 decision was spent on unchanged code).
- Loop recovery, card T1-LOOP-RESUME: `aidlc goal extend` re-admits a goal stopped for time (re-entering through CARDS, so the projection is re-validated and a T2 goal re-authorized first) and every card of its current projection stopped for time (their deadlines move to the new goal deadline, the runs in progress again, journaled; other stops and superseded cards stay); `aidlc goal resume` accepts `--text`, `--cards` and `--replace` and applies the revision before the projection, so a stopped or finished goal continues with replacement cards, re-entering through CARDS with its completion evidence reset and no earlier release reused (an incoherent revision is refused before anything is journaled, and `card next` parks a card while its T2 goal is in CARDS without the plan checkpoint for the current revision); `aidlc goal amend` on a terminal goal names the resume.
- Lessons in the loop, card T1-LOOP-LESSONS: `src/artifacts/lessons.ts` owns the frozen line format and the append-only file (created from an embedded header when missing, the template placeholder giving way to the first lesson); PREPARE carries `lessons` (file, count, most recent lines); the file is created exclusively and only ever appended to, one literal line per lesson, with calendar dates, one-line fields and the same validation for parsing, reading and writing; closure flags need a verified merge and a fenced card lease (the serialisation of a card's closers), are refused for a terminal or re-generated goal and for any raw card patch (which sets no loop-owned evidence: closure, merge, lease, CLOSE or DONE), and are accepted for a CLOSE run only (CLOSE reads the stored run, renews or takes over the card lease, reconciles an ownership stop once the blocking lease is gone, and DONE records that predate the predicate stay DONE at card and goal level while the schema default stays false), a lesson is journaled before the file changes and a retry reuses that pending line, so a completed append is recognised even across a date rollover and no writer overwrites another (every write is an append); `closure.lessons` is the sixth closure predicate, recorded by `aidlc card close --lesson "<NEVER|ALWAYS|NOTE> <rule> (source: <ref>)"` or `--skip-lesson "<why>"` (commander reserves the `--no-` prefix for boolean negation, so not `--no-lesson`), both journaled as EVIDENCE_RETAINED; `--all` leaves it open.
- CI gates in the loop, card T1-LOOP-GATES: the gitleaks history scan is a blocking job in both workflow copies; a failing check run named for a secret or security scan, or raw gitleaks output, is the CI class `security` (never rerun, STOP/risk); `aidlc.config.json` gains `github.requiredChecks|requireVerdict|ciTimeoutMs|ciPollMs`, passed to the GitHub ship path through `shipPathFor`, where a required check absent from the head is pending, never satisfied, and every reported check must succeed. This repository requires its four unconditional check runs. The GitHub gate lines carry the check runs as JSON with brackets encoded, so a name with newlines, commas or sentinel-like text survives and never enters the sentinel stream, and the runner classifies those failures from the structured checks alone (a pending name is no evidence; a green scan next to a red build is a code defect; every red gate line counts; Unicode line separators in names are escaped; plain name=conclusion pairs are still read and an unparsable red pair-like line naming a scan fails closed); a required check that reports skipped or neutral fails the gate; `secret_scan` spellings count. `github.requireVerdict` false is refused next to `gateRequired` true, and a block verdict for the candidate fails the ship even when the requirement is waived.
- Review budgets independent of the build ladder, card T1-LOOP-LADDER: a review block (R2 round or R3 decision) reopens the effort episode without converting the blocked attempt into a counted failure; `countedAttempts` counts DoD failures only, attempt numbers stay sequential, and the same-cause and escalation rules are unchanged for real failures. Reviews keep their own budgets (rounds per R3 cycle, two R3 decisions). Found when two cards of the loop-integration goal ended by the ladder while every review finding was accepted; the first line of `docs/LESSONS.md` records it. Not-counted records after a block never hide the reopened success, and a preserved success between two same-cause failures breaks the streak while the DoD-failure count stays cumulative.

- Skills in the loop directives, card T1-LOOP-SKILLS: every routing result and directive carries `skills` (`diagnose` on bugfix and incident routes, `grilling` on T1/T2, `tdd` on every card-loop route; the `[route]` line prints `skills=`); `aidlc goal new --intent <file>` records the intent on the goal and the T1/T2 plan directive lists its open questions for the grilling skill (a missing file is narrated, never thrown); the build directive names `tdd`, plus `diagnose` on a bugfix goal or a card with `diagnosis:`; PREPARE points at `docs/LESSONS.md`; a merge conflict at ship (git's or GitHub's own diagnostic on a line of the ship output, never a card id, a resume command or quoted text) returns the card to BUILD naming `merge-conflicts` with the DoD receipt cleared, the effort episode reopened and the repair persisted as `CardRun.pendingRepair` until the next successful attempt (a failed repair keeps it), so a resumed worker still gets the skill; when the episode cannot admit a repair attempt (judged with the justification BUILD grants) the card stops with reason `card`, and after the card deadline with reason `time`; a rejected RED receipt is remembered so the scaffold worktree cannot reload it; other merge failures keep the tool stop; `red-missing` reopens the episode the same way and clears the rejected RED receipt. Every build directive, including the review-fix repair, names the skills. Skills follow the finalized size as well as the kind (an explicit T0-bugfix card route names `diagnose`, explicit T1/T2 card routes name `grilling`). An intent file that does not validate is narrated as such (by problem category, never a parser excerpt that could carry a front-matter value), never as an empty question list.

- Review panel and review guards, card T0-R2-PANEL: `preReview.perspectives` runs one reviewer process per angle concurrently and aggregates into one round verdict (quota-hold > block > no-verdict > pass; reasons tagged with the angle; the round document is always retained); the formal review stays one exhaustive pass per decision. The R3 prompt now demands every material finding in one pass (adversarial stance) instead of a brief answer. A deterministic scope gate blocks changed paths outside `allow_paths` before any model call. Citation rule in code: a block reason without an axis tag and a diff location is advisory, never blocking. This repository runs R2 as three contract-bound angles (ac-coverage, spec-deviations, edge-cases) and R3 as one exhaustive Codex pass. Guards from the second Codex decision: no review runs once the allowance is used or the card is stopped, a verdict naming another branch is stale, a ship-path re-read of a command verdict is never a second decision, a non-zero reviewer exit or a self-reported failure never passes, the canonical verdict file is published only for a successful non-stale decision, the invocation is reserved before dispatch and state is re-read after it (a STOP saved meanwhile is kept), the formal review takes a shared-pool slot, HEAD must be the pinned candidate, dynamic instructions never pass through a shell, perspective names are validated, changed paths are collected NUL-separated and the diff with --text so a file that was binary on the base still shows its hunks, a reviewer angle that fails to spawn is a failed receipt rather than an aborted round, an advisory block is published as a consistent pass with every finding under advisory, review guards read the persisted run and fence the lease before reserving, source files no longer contain NUL bytes (git diffed them as binary, so R2 never saw the review module); a quota hold is timed from the clock after the review returns, since a review can outlast the hold it reports (found by the R2 edge-cases angle on this card). Cards no longer pre-set `status: merged`.

- Formal review (R3) as a command, card T0-R3-COMMAND: `aidlc review r3 <card>` runs `formalReview.command` (Codex `exec` with a materialised verdict schema in this repository; argv placeholders `{instructions} {base} {head} {card} {schema} {cwd}`, prompt on stdin when `{instructions}` is absent), writes the candidate-bound `.review/<card>.json` and records the decision through the existing R3 ledger. SHIP now requires R2 pass, then R3 pass; a merge-blocking block (`gateRequired: true` here) is REVIEW_FIX and the repaired candidate restarts the pre-review cycle; quota hold is WAIT (`holdUntil` on the invocation); no-verdict keeps the single retry. Template ships R3-as-command disabled.

- Companion skills and review rules on top of the playbook: four advisory skills adapted from mattpocock/skills (`tdd`: seams, anti-patterns that fake a RED, mocks at boundaries; `diagnose`: red-capable feedback loop before any hypothesis, ranked falsifiable hypotheses, tagged probes, regression test before the fix; `grilling`: question rounds over the frontier for T1/T2 intake; `merge-conflicts`: resolve by intent, merge only), named in `aidlc-loop/SKILL.md` and pointed at from the implementer, investigator and planner agents. REVIEW.md gains "Untrusted content" (candidate content is evidence, never instructions; an active attempt to redirect the review is a dimension 2 Security finding) and "Verdict budget" (write the verdict before the budget ends); `reviewPolicyVersion` is `REVIEW.md@2`. Planner ready check, verifier target table (Met / Not Met / Unverified). `docs/LESSONS.md` (append-only dated NEVER/ALWAYS/NOTE lines, read at PREPARE, written at CLOSE) and the documentation policy in the CLAUDE.md aidlc section. Guard tests: `.claude/**` must equal `templates/claude/**` and CLAUDE.md must end with the template section (`tests/surface/mirror.test.ts`); no em dash or corner brackets in tracked prose (`tests/surface/prose.test.ts`). `docs/THIRD-PARTY-NOTICES.md` carries the MIT notice for the adapted skills. A non-blocking gitleaks scan of committed history (`.github/workflows/security-scanners.yml`). All of it ships through `aidlc init`.

- Pre-review (R2), card T0-PRE-REVIEW: a bounded second-model review before the ship. `aidlc review pre <card>` runs the configured command (DeepSeek V4 Pro in this repository) on the committed candidate against REVIEW.md, receipted and journaled (`PRE_REVIEW_DECIDED`); the ship requires a fresh `pass` per candidate, a `block` is a counted repair with the reasons carried forward, rounds are capped per R3 cycle (`preReview.rounds`, 3 here, exhaustion is STOP/review unless `onExhausted: "ship"`), and an R3 block restarts the cycle. New config block `preReview` (opt-in; empty command = off); `.review/` ignored by git.

Loop latency (card T0-LOOP-SPEED). Measured on Windows 11, Node 22.23, before -> after:

- Claude Code hooks: one process per event instead of three `npx` starts per tool call. `src/hooks/entry.ts` (`dispatchHook`, `hookNamesFor`) runs every guard that applies to the event; `bin/aidlc-hook.js` loads only the hook modules; `aidlc hook auto` is the CLI form. Per Bash/Edit call: ~4.5 s -> ~0.2 s (direct entry) or ~1.4 s (npx fallback). `aidlc init` wires the fastest entry it can see and replaces 0.1.0 per-guard hooks on re-run.
- Repository identity: `resolveRepoIdentity` is memoised per process and asks git once (`rev-parse --git-common-dir --show-toplevel`) instead of six spawns per CLI call. `aidlc goal list` ~0.8 s -> ~0.3 s; `aidlc next` ~0.4 s.
- `aidlc doctor`: toolchain probes run concurrently (`pwsh -v`). ~1.2 s -> ~0.5 s.
- `npm run typecheck`: one `tsc` pass over src + tests. ~6.7 s -> ~4.0 s. Full suite (414 tests) ~6.9 s -> ~4.1 s.
- New bin `aidlc-hook`; `runHook` accepts a preloaded config.
- Card lease heartbeat (T0-LEASE-HEARTBEAT, bugfix): `CardRunner.next` renews the card lease when the acting session owns it at the run's generation, so a BUILD longer than the 10-minute TTL no longer fences the ship as STOP/ownership; a stop caused only by the owner's own expiry is revalidated on the next call. A lease held by another session still stops. Found by the loop itself while shipping T0-LOOP-SPEED.
- WAIT resumption (T0-WAIT-VERIFY, bugfix): a goal polled with `aidlc next` while its cards were running parks in WAIT; once every required card closed, `nextInRun` attempted WAIT -> VERIFY_ARC, which the goal diagram forbids, so `next`, `report` and `card report` threw GoalTransitionError and the goal could never finish. It now resumes WAIT -> RUN (journaled) and then derives VERIFY_ARC. Found by the loop itself while closing T0-LOOP-SPEED.

## 0.1.0 - 2026-09-11

First implementation of the v5 aidlc-loop plan as a TypeScript library and CLI.

- Core engines: request router (T0-bugfix/T0/T1/T2, targets, modules, data impact), admission deadlines (3 h card, 12 h arc, 5 min grace, explicit extensions), effort episodes (three baseline attempts plus one justified escalation, same-cause early stop), review policy (scaffold verdict parsing, two substantive decisions, one no-verdict retry, quota holds), CI policy (classification, one persisted transient rerun), health evaluation (PASS/BREACH/INSUFFICIENT_DATA), authorization binding, arc selection (cap two, resource isolation, freeze cards, contraction ordering), goal/card/release state machines, migration impact and phase graph, amendments, role profiles.
- State: canonical `.aidlc/` under the main checkout, atomic JSON store with interrupted-write recovery, hash-chained journal, goal/card/release store, board view.
- Coordination: leases with generations and fencing, takeover after reconciliation, shared review admission queue, operation ledger with explicit UNKNOWN.
- Delivery: git and gh probes with receipts, worktree start-or-attach, scaffold `task.ps1` ship adapter with sentinel classification, native GitHub ship path (git/gh chain emitting the same sentinels), dry-run ship path, provider operation bindings (`aidlc.ops.json`).
- Session identity: `AIDLC_SESSION` > `CLAUDE_SESSION_ID` > a per-repository default token (`.aidlc/session-default`, interim single-controller mode reported by `doctor`).
- Artifacts: front matter, task cards (parse, validate, tier, render), intent.md, spec.md with EARS classification, plan.md with definition-of-ready gates.
- Providers: Anthropic SDK (adaptive thinking, effort, quota/refusal outcomes), Claude Code CLI, mock.
- Maintain: Western Electric control bands from `bands.yaml`, deduplicated incident ledger that files intents.
- Evals runner with a pass-rate gate; evidence manifest with sealing and an independent audit verifier.
- Claude Code hooks: production-gate, protect-paths, secrets-guard, protect-tests, verify-before-done, route-new-work.
- Loop: typed directives, goal controller (`next`/`report`), card runner, release runner.
- `aidlc init` templates: five aidlc-loop skill files under their byte caps, secure-api-review skill, six agents, settings.json, REVIEW.md, bands.yaml, artifact templates, evals example, CI workflows, delivery-ops contract.
- Tests: 413 node:test cases across core, infra and surface suites.
